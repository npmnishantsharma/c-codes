#include<iostream.h>
#include<conio.h>

void main(){
clrscr();
int a,b;
cout<<"Enter a number: ";
cin>>a;
if(a%b == 0)
  cout<<"first number is divisible by second";
else
  cout<<"first number is not divisible by second number";
getch();
}